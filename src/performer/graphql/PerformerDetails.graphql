query PerformerDetails($id: ID!) {
  performer(id: $id) {
    id
    name
    albums {
      nodes {
        ...AlbumGridItem
      }
    }
    details {
      bio {
        content
        published
        summary
      }
    }
  }
}
