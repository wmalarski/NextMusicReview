query PerformerDetails($id: ID!) {
  performer(id: $id) {
    id
    name
    albums {
      nodes {
        ...AlbumListItem
      }
    }
    details {
      bio {
        content
        published
        summary
      }
    }
  }
}
